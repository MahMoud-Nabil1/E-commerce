services:
  # ── Backend: Spring Boot ──────────────────────────────────────────────────
  - type: web
    name: ecommerce-backend
    runtime: java
    rootDir: ecommerce
    buildCommand: ./mvnw clean package -DskipTests
    startCommand: java -jar target/ecommerce-0.0.1-SNAPSHOT.jar
    plan: free
    envVars:
      - key: DB_Password
        sync: false          # set manually in Render dashboard
      - key: JWT_SECRET
        sync: false
      - key: GOOGLE_CLIENT_ID
        sync: false
      - key: GOOGLE_CLIENT_SECRET
        sync: false
      - key: GITHUB_CLIENT_ID
        sync: false
      - key: GITHUB_CLIENT_SECRET
        sync: false
      - key: OAUTH2_REDIRECT_URI
        sync: false          # e.g. https://your-frontend.onrender.com/oauth2/callback
      - key: CORS_ALLOWED_ORIGINS
        sync: false          # e.g. https://your-frontend.onrender.com
      - key: COOKIE_SECURE
        value: "true"
      - key: COOKIE_SAME_SITE
        value: "None"
      - key: STRIPE_SECRET_KEY
        sync: false
      - key: SPRING_JPA_HIBERNATE_DDL_AUTO
        value: update

  # ── Frontend: React/Vite static site ─────────────────────────────────────
  - type: web
    name: ecommerce-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    plan: free
    envVars:
      - key: VITE_API_BASE_URL
        sync: false          # e.g. https://ecommerce-backend.onrender.com
    routes:
      # Send all routes to index.html so React Router handles them client-side
      - type: rewrite
        source: /*
        destination: /index.html
